hibernate: 자바와 관계형 데이터베이스를 연결해주는 ORM
JPA 표준 명세를 구현한 구현체
flyway: DB 스키마 변경 이력 관리(git처럼)

tomcat: web container
Catarina: servlet의 동작 컨테이너 / Casper: jsp의 동작 컨테이너
tomcat 위에 springMVC가 올라가있음
spring의 container, xxxMapping이 생략되어있음
token: redis에 관리
redis는 doker에 만들기
토큰의 유효성 관리는 Filter에서(DispatcherServlet에 들어오기 전에)
interceptor or aop: DispatcherServlet과 프레임워크 사이에 있는 filter


https://www.base64decode.org/
인코딩-디코딩 시연

토큰을 관리할 수 있는 키가 필요
-> .env 파일에 임의로 선언

@Value: yml에 설정된 값 읽어오기

읽어온 키를 hmacShaKeyFor로 인코딩 시킴
Jwts.builder()를 반환
- 서명 주체
- 서명 시각
- 유효 기간
- 인코딩된 서명

filter에서 cors와 토큰에 관한걸 다 처리하게 할 수 있음(어차피 프론트엔드의 요청은 필터를 거치니까)

지금은 Spring security가 없으니 jwtfilter.java로 구현
doFilter: 요청이 들어오면 무조건 실행되는 콜백 함수
얘를 통과해야 DispatcherServlet으로 갈 수 있음

@PostConstruct: 생성자 호출 바로 뒤 실행될 메서드
(생성자가 없으면 얘를 붙인 메서드가 생성자(?))

dofilter로 요청이 들어옴(chain은 dispatcherservlet
-> options로 변경
-> header set 진행
-> 다시 dofilter로 넣기
-> 화이트리스트에 있는 엔드포인트에 접속 중이면 그냥 보내기,
    화이트리스트에 없는 엔드포인트에 접속 중이면 헤더의 토큰을 확인
    -> 토큰이 있고 유효하면 Dispatcherservlet으로 보내기,
        없으면 쳐내기

필터를 여러개 만들어서 엔드포인트별로 적용하는 것도 가능

Repository 안에 JPQL을 작성할 수도 있음(엔티티를 기반: select * from entity...)

blogservice의 write: request의 이메일로 select -> BlogEntity(toEntity(UserEntity(email))) -> save()
blogrequestdto와 blogresponsedto -> 중요함
(BlogEntity의 writer가 userEntity이기 때문에 mybatispractice와는 조금 다름)

blogservice의 read
글 불러오고 댓글을 불러오면 쿼리를 두번 호출 -> 성능에 문제가 생김
두 작업을 한번에 처리하도록

jpa로 update할 때
- dirty checking(변경 내용 감지)
entity의 내용을 변경하고 transaction을 발생시키지 않으면 변경 내용이 db에 반영이 안됨
현재 상태와 이전 상태에서 차이가 있으면 update 발생
stream의 중간 연산
filter
map
flatmap: 중첩 구조를 평탄화
sorted
distinct: 중복 제거
limit
skip
...

stream의 최종 연산: 하고 나면 stream 재사용 불가
collect
forEach
count
reduce
...
findFirst: 순서 보장
findAny


Optional: 하나의 wrapper class
NullPointerException을 회피하는 방법
메서드 호출 시 반환값을 확신할 수 없을 때 쓰는 문법
메서드의 반환 타입으로만 쓸 수 있음
optional에 일부러 null은 넣지 말기
isPresent()로 null인지 아닌지 확인하고 get()으로 값을 꺼낼 수 있음

fileDB
stream input output

controller: 여러 파라미터를 객체로 묶고 데이터의 유효성을 체크하는 역할

타입변경
RequestDTO로 쭉 데이터를 보내다가 마지막에 타입을 바꿔줘야함 -> 정적 메서드 패턴
자기 자신을 입력받아서 ResponseDTO로 바꿔버림(원래는 Entity로 바꿔야하지만 지금은 ResponseDTO로 변경)
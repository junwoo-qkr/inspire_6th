RDS의 종류
Oracle의 DB
MySQL
MariaDB
DB2
tibero

noSQL의 종류
Casandra
MongoDB

MySQL이 깔린 상태로 MariaDB를 깔면 포트가 3306이 아니라 3307로 바뀜

DBMS: 물리적인 DB를 관리하는 프로그램


SQL 명령어
DDL: Data Definition Language(데이터 정의어)
- CREATE
- ALTER
- DROP
DML: Data Manipulation Language(데이터 조작어)
- SELECT
- INSERT
- UPDATE
- DELETE
DCL: Data Control Language(데이터 제어어)
- COMMIT
- ROLLBACK

데이터는 table이란 객체에 저장됨
modeling(개념적, 논리적, 물리적)으로 table을 생성

MariaDB 설치함
세션 만듦

마우스 오른쪽 클릭 -> 새로 생성 -> 데이터베이스 를 통해서 데이터베이스 생성 가능
데이터베이스 안에 table이 실제 데이터를 관리하게 됨
파일 -> sql파일 불러오기 -> book.sql 가져오기

use 데이터베이스 -> 데이터베이스 옮기기

데이터베이스
여러 응용 시스템에서 액세스가 가능한 데이터의 집합

데이터: 관찰, 특정을 통해 수집된 값
정보: 데이터를 처리해 얻어진 결과

DB의 특징
데이터의 독립성 유지
데이터의 무결성 유지
데이터의 불일치 배제

옛날엔 파일 시스템을 쓰다가 이젠 DBMS를 씀

dbms의 유형
계층형
네트워크형
관계형: 수학적 논리 관계를 테이블 형태로 구현

db는 최소한의 의미를 가지는 테이블들로 구성
그 테이블의 필드들로 연결한 것
필드도 가장 작은 논리적인 단위로 구분하기
-> 정규화

관계: 업무적인 연관성
두 테이블 사이에 존재하며 하나는 부모, 하나는 자식
부모의 기본키는 자식의 외래키로 전이

테이블 하나를 엔티티로 간주
기본키: 테이블을 대표하는 컬럼
null값 X, 중복 허용 X
JOB 테이블의 기본키: EMPLOYEE 테이블의 외래키
db 설계 시 참조 무결성이 제일 중요함

L자형 테이블
null값이 들어있는 값들을 위로 올려버린것
레코드를 기준으로 테이블 나누기(자동차가 있는 사람 테이블과 없는 사람 테이블로 나누기)
컬럼을 기준으로 테이블 나누기(개인 신상 테이블과 자동차 테이블로 나누기) -> 자동차가 누구 꺼인지 모름
-> 두 테이블 사이에 관계를 만들기

관계의 유형
- 존재의 유형(부서와 사원의 관계)
- 행위의 유형(고객과 주문의 관계)

FK는 null을 가질 수 있음(ERD에서 Optional이 있으면)

참조 무결성
부모 테이블의 삭제: 참조 무결성이 검증되어야함

varchar
date
int

인덱스
테이블의 row와 하나씩 대응되는 객체
컬럼의 값 + 그 행의 논리적인 주소
인덱스를 사용해서 성능 향상 가능

SELECT 절에 NULL이 연산에 쓰이는 함수가 나오면 NULL값이 그대로 뜸
함수를 쓰면 함수가 그대로 열의 이름으로 나옴 -> AS로 별칭 붙이기(AS 생략 가능)

나중에 erd cloud 쓰기
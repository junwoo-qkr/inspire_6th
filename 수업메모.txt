test하는 클래스에서는 메인의 파일에 접근 가능

web application server에 올릴 때 소스코드, 라이브러리 등을 묶어서 .jar로 패킹

도메인 당 하나의 mapper xml 할당

컨트롤러는 서비스를 의존성으로 주입받음

@RequiredArgsConstructor: 멤버 변수 중 상수(final)만 인자로 받는 생성자 만듬 -> 생성자를 통한 의존성 주입

.env 파일을 만들어서 민감한 내용(비밀번호)나 자주 바뀔 수 있는 내용(url이나 id)를 관리할 수 있음
Dotenv: .env 파일의 내용을 .yml에서 참조할 수 있도록 메모리에 올리기
gitignore에 .env 추가

http://localhost:8000/swagger-ui/index.html
들어가면 api 명세가 보임(swagger를 넣었기 때문)
@Tag: api의 이름과 설명 추가
@operation: 엔드포인트에 대한 설명 추가
@apiresponse(s): 응답에 대한 설명 추가
swagger의 @requestbody: requestbody에 대한 정보 추가

파라미터를 받는 방법
@requestparam: querystring의 정보를 받음
@PathVariable
@RequestBody + DTO 객체: json으로 들어오는 데이터를 DTO로 받을 때

bean이 관리하지 않는 객체를 컨트롤러에 등록하면 내부적으로 객체 생성(기본 생성자)

@mapper가 달린 mapper 인터페이스는 bean으로 관리가 안됨(클래스가 아니기 때문에)

@Configuration: yml에 설정해야하는 환경설정을 java 코드로 하기

인증, 인가의 차이?

jwt
createAT, createRT는 인풋으로 서명을 받음(해당 사용자의 키 값)
로그인 시 예외가 터지지 않으면 토큰 발급, Map에 담아서
토큰이 발행되면 유지되어야 함(OAuth 하는게 아니면 inMemoryDB에 담기)
access token은 authorization으로, refresh token은 refresh-token으로 httpheaders에 담기
로그인이 완료된 사용자에 대해서는 authorization을 헤더에 담아서 줘야함
signIn을 get 방식으로 쓰면 에러가 나는 이유
- get 방식은 json을 쓰는게 아니라 querystring을 씀
- 파라미터가 @RequestBody로 들어와서 json을 받아야 해서 에러가 남
-> 파라미터를 @RequestParam으로 받기
-> 추후에 post 방식으로 바꾸기(주소에 아이디 비밀번호가 다 나오기 때문에)

json을 dto로: @RequestBody

리액트 프로젝트의 endpoint를 8000번으로 바꾸고 url 변경
cors 정책 때문에 연결이 안됨
-> cors 정책을 해결하는 인터페이스 사용(WebMvcConfigurer)
@Configuration

preflight
Options가 요청을 허락하면 post, get, patch 등으로 넘어감

signIn에서 queryString으로 넘기든 param 방식으로 넘기든 작동함
- 어떤 걸로 하던지 결국은 queryString 방식으로 데이터를 보내기 때문

header로 보낸 access token, refresh token을 localstorage에 저장하기
오늘 배우는거: redis, spring security
security 기반의 토큰을 생성하고, redis에 넣기
redis : key: value로 데이터를 담는 inmemorydb 중 하나(리눅스에서 동작)

devops
클라우드 마이그레이션
스케일 아웃
코드를 통한 인프라 관리

베어메탈: 서버에 서비스를 그대로 올려놓음
하이퍼바이저: 서버에 서비스를 vm에 넣어서 올려놓음
컨테이너: 서버에 서비스를 컨테이너에 넣어서 올려놓음

도커: 컨테이너 기반 가상화 서비스
os, 설정, 서비스를 묶어서 하나의 이미지 생성 -> 컨테이너에서 실행

docker hub에서 redis 이미지 가져올 명령어
docker run -d --name redis-container -p 6379:6379 redis:7.2
docker exec -it redis-container redis-cli
docker run -d --name redis-insight -p 8001:5540 redis/redisinsight:latest
(redisinsight: redis에 대한 ui 툴)
-> 8001번 포트로 redisinsight에 접근 가능

host를 host.docker.internal로 설정해서 redis database를 추가

redis의 TTL(Time To Live, 유효기간)은 초 단위로

redisconfig: 서버 기동 시 redis와 연결을 위한 설정
RedisConnectionFactory: 연결을 관리하는 factory
RedisTemplate 객체는 뭐지?

@Bean: 프레임워크에게 해당 메서드를 관리하라고 알려주는 어노테이션

redis를 통해 토큰에 더해서 2 way factor, 쿠폰 유효기간 등을 관리 가능

수업에서는 백엔드에서 댓글의 기본키를 commentId로 바꿔서 프론트와 변수 이름이 달라 오류가 났는데, 나는 그냥 id로 써서 오류가 나지 않음
-> 백엔드, 프론트엔드 댓글 기본키를 commentId로 수정

......

security 인증 정보를 담을 수 있는 객체
principal, credential, authorities
UsernamePasswordAuthenticationToken
SecurityContextHolder에 user의 아이디

사용자의 정보를 SecurityContextHolder에 저장하고 필요하면 controller나 service에서 로드해 정보 비교

onceperrequestfilter: spring security 내부적으로 filterchain으로 많은 필터를 거쳐서 dispatcherservlet으로 보냄
security form filter 때문에 localhost:8000/login의 화면이 원래와 다름

hashing: 복호화 안됨, 단방향
encryption: 키가 있으면 복호화 가능, 양방향 -> 비밀번호 저장에 적합하지 않음

corsconfig 주석처리하기

securityconfig
url 당 서로 다른 configuration 적용 가능
cross-site request forgery(csrf): 공격자가 사용자가 의도하지 않은 요청을 보내도록 하는 것을 방지
permitAll(): 토큰 없이도 허용

if("OPTIONS"... 주석처리해도 작동함

blogservice의 글 작성: 요청의 이메일을 들고옴 -> 현재 로그인된 사용자의 contextholder로부터 이메일을 가져오도록 변경


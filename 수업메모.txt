세션, 토큰: 클라이언트의 이전 상태 정보를 유지하기 위한 것
옛날에 세션을 쓸 때...
- 서비스에서 세션 아이디를 제공
- msa로 가니 여러 서비스가 쪼개짐
- 세션 아이디만으로는 서비스 간에서 인증 정보를 유지하기 힘들어짐
- (A 서비스에서 받은 세션 아이디를 B, C 서비스에서 쓸 수 없음)

이젠 jwt(토큰) 사용
- header, payload, signature로 이루어짐
- 응답의 헤더에 넣어야함
- 헤더에 넣었으니 헤더를 열어줘야함

xml에 함수를 쓸 때는 #{}는 빼기(nextval 같은거)

blogcontroller에서 requestbody를 잘못 import 함

(TIL 요구사항: 오늘 수업에서 사용된 테이블(testdb의 spring_post_tbl, spring_user_tbl)도 TIL에 포함시키기)

백엔드와 통신하는 영역: blogitem의 return의 moveurl이 아니라 blogreadpage의 loaddata의 api.get

인증된 사람만 특정 엔드포인트에 접속하도록 하는 방법: api.get의 두번째 파라미터로 토큰 넣어주기

interceptor: 만료된 토큰을 받으면, refresh token을 이용해 토큰 재발급하는 과정

getmapping에서 path variable을 받으려면 /read/{postid}, 매개변수에 @pathvariable 사용

댓글 정보는 댓글에 관련한 정보를 담은 DTO로 이루어진 list가 되어야 함
- 한 트랜잭션에 글 select, 댓글 select를 해야함
- @Transactional을 사용
- readonly = true : 가져오는 동안 누가 수정해버리는 걸 방지

@RequestHeader: 헤더의 정보를 매개변수로 넣을 수 있음(토큰 같은 거)

포스트 하나에 댓글을 여러개 매핑하는 방법: Method 1, Method 2
blogresponsedto에 comments 선언
blogservice의 read에서 blogresponsedto를 복사(builder의 tobuilder를 true로)하고 거기에 comment만 추가
(tobuilder: 원래 객체를 복사해서 다시 빌드)
-> 포스트와 댓글을 불러오는 걸 하나의 트랜잭션으로 처리

selectKey로 commentrequestdto에서 id가 안 넘어와도 sql문에서 id를 만들 수 있음 -> dto에 id를 넣어버림
commentservice의 write에서 id를 반환할 수 있음
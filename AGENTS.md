# TIL 작성 규칙

이 프로젝트에서 사용자가 수업 메모와 오늘 수업의 중점 내용을 제공하며 TIL 작성을 요청하면 아래 규칙을 따른다.

## 작성 기준

- 프로젝트 루트의 `TIL템플릿.md` 구조와 항목 순서를 그대로 사용한다.
- 사용자가 참고할 수업 메모를 별도로 지정하지 않으면 프로젝트 루트의 `수업메모.txt`(`F:\inspire_6th\수업메모.txt`)를 참고한다. 사용자가 다른 수업 메모를 지정하면 지정한 메모를 우선한다.
- 수업 메모가 단편적이거나 순서가 뒤섞여 있더라도, 사용자가 알려 준 **오늘 수업의 중점 내용**을 중심 주제로 삼아 내용을 재구성한다.
- 메모에 적힌 사실과 작업 환경에서 확인한 사실만 사용한다. 불확실한 내용을 지어내지 않는다.
- `제목:` 뒤에는 오늘 수업의 중점 내용을 나타내는 구체적인 제목을 작성한다.
- `오늘의 한 줄 요약`에 들어가는 요약의 개수는 고정하지 않는다. 수업 메모의 범위와 내용에 따라 필요한 만큼 목록으로 작성한다.
- `오늘의 한 줄 요약`과 `오늘 배운 것` 사이에는 `키워드 정리` 영역을 작성한다.
- `키워드 정리`에는 수업 메모와 참고 코드(현재 미커밋 코드 또는 사용자가 지정한 코드)에서 확인할 수 있는 새로운 문법, 새로운 개념 등 중요하다고 판단되는 키워드를 선별한다. 자료에서 확인되지 않는 키워드는 임의로 추가하지 않는다.
- `키워드 정리`는 `키워드`, `설명 또는 예시` 열을 가진 마크다운 표로 작성한다. 개념 키워드는 핵심 설명을, 코드나 문법 키워드는 짧고 일반화 가능한 코드 예시를 `설명 또는 예시` 열에 작성한다.
- 표에 넣는 코드 예시는 한눈에 이해할 수 있도록 인라인 코드 형식으로 간결하게 작성하고, 긴 코드나 파일 전체를 넣지 않는다.
- `오늘 배운 것`에 들어가는 학습 주제의 개수는 고정하지 않는다. 수업 메모를 중점 내용에 맞게 정리해 필요한 만큼 주제별 영역을 만든다.
- `오늘 배운 것`의 내용이 코드 예시를 통해 일반화할 수 있는 개념이고, 그 예시가 블로그에 작성하기에 적합할 정도로 충분히 짧다면 반드시 언어가 표시된 코드 블록으로 예시를 포함한다. 스프레드 연산자나 옵셔널 체이닝처럼 간결하게 표현할 수 있는 개념이 이에 해당한다.
- HTML5 표준 템플릿처럼 예시 전체가 길어지는 코드는 `오늘 배운 것`에 억지로 포함하지 않고 개념 설명으로 정리한다.
- 각 영역은 `### 구체적인 학습 주제명` 형식의 소제목으로 시작한다. `학습 주제 N`과 같은 템플릿용 표현을 완성된 TIL에 그대로 사용하지 않는다.
- 각 학습 주제 아래에서는 역할이나 성격이 비슷한 개념을 각각 나열하지 말고 하나의 목록 문단으로 묶어 설명한다. 예를 들어 `for`, `forEach`, `while`은 반복문 주제로 묶고, `if-else`, `switch`는 조건문 주제로 묶는다.
- 종류, 차이, 역할, 사용 방법처럼 여러 항목을 비교하는 편이 이해하기 쉬운 내용은 마크다운 표로 정리한다. 비교할 필요가 없는 내용에 표를 억지로 사용하지 않는다.
- 서로 성격이 다른 개념은 별도의 학습 주제로 나누고, 각 주제 영역 사이에는 구분선(`---`)을 넣어 구분이 드러나게 작성한다.

## 오늘의 코드 작성

- 연결된 작업 환경에서 현재 커밋되지 않은 파일과 diff를 확인하고, 사용자가 별도로 지정한 파일이 있으면 해당 파일도 확인한다. 이 가운데 오늘 수업에서 사용한 파일만 `오늘의 코드`에 포함한다.
- TIL 템플릿, `AGENTS.md`, Git 설정 등 수업 실습물이 아닌 관리 파일은 제외한다.
- 서로 구분되는 오늘 배운 개념마다 `오늘의 코드` 영역을 하나씩 만들고, `N`은 `1`, `2`, `3`처럼 1부터 시작하는 숫자를 순서대로 사용한다.
- 제목은 `오늘의 코드 N - 구체적인 학습 개념명` 형식을 사용한다.
- 같은 개념을 실습한 코드가 여러 파일에 있으면 파일의 연동 여부와 관계없이 하나의 영역에 묶는다. 반대로 하나의 파일에 서로 다른 학습 개념의 코드가 있으면 개념별 영역으로 나눈다.
- 각 코드 블록 바로 위에는 출처 파일 이름을 확장자까지 표시해, 어떤 파일에서 가져온 코드인지 알 수 있게 한다.
- 미커밋된 파일 또는 사용자가 지정한 파일의 코드와 수업 메모를 비교해, 오늘 배운 내용을 실습하는 데 직접 사용된 부분만 언어가 표시된 코드 블록으로 바로 출력한다. 관련 없는 코드나 파일 전체를 불필요하게 출력하지 않으며, `코드 블록`이라는 레이블은 출력하지 않는다.
- `구현 사진`은 템플릿의 작성 위치를 나타내는 플레이스홀더이다. 사용자가 별도로 요청하지 않으면 완성된 TIL에 `구현 사진`이라는 레이블, 실제 이미지 또는 이미지 링크를 출력하지 않는다.
- 코드와 수업 메모를 비교해 수업 내용 중 무엇을 중점적으로 실습했는지 짧은 한마디를 작성하되, `짧은 한마디`라는 레이블 없이 문장만 코드 다음에 출력한다.
- 이미 커밋된 파일은 사용자가 따로 포함해 달라고 하지 않는 한 근거로 사용하지 않는다.
- 오늘 수업과 관련된 미커밋 코드 파일이나 사용자가 지정한 파일이 없으면 파일이나 코드 영역을 임의로 만들어 내지 않는다.

## 어려웠던 점과 오류

- `어려웠던 점 / 각종 오류`는 수업 메모와 해당 채팅에서 사용자가 질문한 내용을 참고해 작성한다.
- 질문 내용을 확인할 때는 사용자가 어려워한 개념과 발생한 오류를 구분하며, 질문에서 확인되지 않는 내용을 추측해 추가하지 않는다.
- 확인된 어려운 점과 오류는 예시처럼 자연스러운 문단과 필요한 코드로 바로 작성하며, `어려운 내용`이나 `오류 내용`이라는 레이블은 출력하지 않는다.
- 어려운 점 또는 오류 중 확인되지 않은 항목은 플레이스홀더나 빈 목록 기호를 남기지 않고 생략한다. 둘 다 확인되지 않으면 `어려웠던 점 / 각종 오류` 제목만 유지하고 본문은 비워 둔다.

## 출력 방식

- 코드는 `키워드 정리`의 짧은 예시, `오늘 배운 것`의 짧고 일반화 가능한 예시, `오늘의 코드`에서 학습 내용의 실습에 직접 사용된 부분에 한해 출력한다. 그 밖의 코드나 전체 파일 내용은 TIL 본문에 출력하지 않는다.
- 템플릿의 HTML 주석은 작성 위치를 안내하는 플레이스홀더이므로 완성된 TIL에는 출력하지 않는다.
- TIL 작성 요청을 받으면 완성된 내용을 마크다운 형식으로 생성하고 프로젝트 루트의 `TIL` 폴더에 저장한다. `TIL` 폴더가 없으면 새로 만든다.
- 파일 이름은 수업 날짜를 기준으로 `MM월DD일 TIL.md` 형식을 사용한다. 사용자가 수업 날짜를 별도로 지정하지 않으면 TIL 작성 요청을 받은 날짜를 사용한다. 예: `09월02일 TIL.md`
- 사용자가 별도로 파일 생성을 요청하지 않은 경우 완성된 TIL을 대화에 출력한다.
- 설명이나 머리말을 덧붙이지 않고 템플릿 형식의 완성된 TIL부터 제시한다.

# 스터디 실습 가이드 작성 규칙

이 프로젝트에서 사용자가 수업 내용을 바탕으로 스터디 발표용 또는 따라 하기용 실습 가이드 작성을 요청하면 아래 규칙을 따른다.

## 참고 자료와 작성 근거

- 프로젝트 루트의 `스터디_실습가이드_00월_00일.md` 구조와 항목 순서를 기본으로 사용한다.
- 특정 일자의 스터디 실습 가이드 생성을 요청받으면 프로젝트 루트의 `TIL` 폴더에서 해당 날짜의 `MM월DD일 TIL.md` 파일을 찾아 주된 작성 근거로 사용한다.
- 요청한 날짜의 TIL 파일이 없으면 수업 메모나 다른 날짜의 TIL로 대신하지 않고 스터디 실습 가이드를 생성하지 않는다. 이 경우 누락된 TIL의 예상 경로를 사용자에게 알린다.
- 특정 일자를 지정하지 않은 요청에서 사용자가 참고할 수업 메모를 별도로 지정하지 않으면 프로젝트 루트의 `수업메모.txt`를 참고하며, 다른 메모를 지정하면 지정한 메모를 우선한다.
- 현재 미커밋 코드와 diff를 확인하고, 사용자가 별도로 지정한 코드가 있으면 함께 확인한다. 해당 날짜의 TIL 또는 참고 수업 메모와 연결되는 코드만 실습 가이드의 근거로 사용한다.
- 해당 날짜의 TIL, 수업 메모, 해당 채팅에서 확인된 내용, 참고 코드에서 확인할 수 있는 사실만 작성한다. 불확실한 설명이나 실제로 확인되지 않은 실행 결과를 지어내지 않는다.
- 템플릿, `AGENTS.md`, Git 설정 등 실습물이 아닌 관리 파일은 실습 코드에 포함하지 않는다.

## 전체 구성

- 제목은 `# 스터디 실습 가이드 — MM월 DD일: 구체적인 핵심 주제` 형식으로 작성한다.
- 도입부에는 가이드의 목적을 한 문장으로 작성한다. 예상 소요 시간은 수업 메모나 사용자가 제공한 진행 계획에서 확인할 수 있을 때만 작성한다.
- 학습 목표는 `설명할 수 있다`, `비교할 수 있다`, `구현할 수 있다`, `오류를 재현하고 해결할 수 있다`처럼 실습 후 확인 가능한 행동을 중심으로 작성한다.
- `진행 순서 한눈에 보기`에는 개념 설명과 실습이 이어지는 순서를 간단한 번호 목록으로 정리한다.
- 서로 구분되는 학습 단위를 `## Part N. 구체적인 학습 단위명`으로 나누고, 각 Part 안에서 필요한 만큼 `### Step N-N. 구체적인 실습명`을 순서대로 만든다.
- 트러블슈팅, 발표 데모 시나리오, 발표·토론 질문, 확장 과제처럼 근거 또는 필요성이 없는 선택 영역은 빈 제목이나 플레이스홀더를 남기지 않고 생략한다.
- 템플릿의 HTML 주석과 `N`, `00월 00일`, `학습 주제` 같은 플레이스홀더는 완성된 가이드에 출력하지 않는다.

## 개념 설명과 실습 작성

- 각 Part의 `핵심 개념`에는 해당 실습을 이해하는 데 필요한 내용만 간결하게 설명한다. 정확성을 해치지 않는 범위에서 비유를 사용할 수 있다.
- 종류, 차이, 실행 시점, 장단점처럼 비교가 중요한 내용은 마크다운 표로 정리한다.
- 각 Step은 `목표 → 사용 파일 또는 준비 작업 → 코드 → 따라 해보기 → 확인할 것`의 흐름으로 작성해 독자가 순서대로 실행할 수 있게 한다.
- 새로 만들거나 크게 수정하는 파일은 독자가 그대로 저장해 실행할 수 있도록 필요한 전체 코드를 우선 제공한다. 파일 전체가 지나치게 길거나 관련 없는 코드가 많으면 학습에 필요한 부분만 발췌하되, 실행에 필요한 전후 맥락과 의존 파일을 빠뜨리지 않는다.
- 코드 블록 바로 위에는 프로젝트 기준 상대 경로와 확장자를 포함한 파일 이름을 표시하고, 코드 펜스에는 `html`, `css`, `javascript`, `jsx`, `sql`, `json`, `bash`처럼 알맞은 언어를 지정한다.
- 명령어를 제시할 때는 실행할 디렉터리와 명령의 목적을 함께 설명한다. 운영체제나 셸에 따라 명령이 달라지면 현재 작업 환경에 맞는 명령을 우선한다.
- `따라 해보기`에는 작성, 실행, 값 변경, 주석 해제, 전후 비교처럼 독자가 실제로 수행할 행동을 번호 순서로 작성한다.
- `확인할 것`은 화면, 콘솔, 네트워크 응답, 오류 메시지 등 관찰 가능한 결과를 체크박스로 작성한다.
- 오류나 동작 차이를 배우는 실습은 필요할 때 `Before`와 `After` 코드를 나란히 제시하고, 재현 방법과 수정 후 기대 결과를 함께 설명한다.
- 발표 포인트는 코드의 핵심 흐름과 실수하기 쉬운 부분을 짧게 짚는다. 예상 질문과 답변 및 토론 질문은 해당 수업에서 다룬 개념과 코드만으로 답할 수 있게 작성한다.

## 문제 해결과 마무리

- `트러블슈팅 케이스`에는 해당 날짜의 TIL, 수업 메모, 해당 채팅의 질문 또는 참고 코드에서 실제로 확인된 문제만 사용하고, 각 사례를 증상·원인·해결 순서로 작성한다.
- `전체 체크리스트`에는 학습 목표와 각 실습의 성공 조건을 대응시켜, 발표 또는 실습 전에 스스로 확인할 수 있는 항목을 작성한다.
- 확장 과제는 본문에서 배운 개념을 한 단계 응용하는 범위로 제한하고, 아직 배우지 않은 기술을 필수 조건처럼 추가하지 않는다.
- 마지막에는 전체 학습 흐름을 연결하는 `마무리 한 줄 요약`을 작성한다.

## 저장 및 출력

- 완성된 가이드는 마크다운 형식으로 프로젝트 루트의 `스터디 실습 가이드` 폴더에 저장한다. 폴더가 없으면 새로 만든다.
- 파일 이름은 수업 날짜를 기준으로 `스터디_실습가이드_MM월_DD일.md` 형식을 사용한다. 사용자가 수업 날짜를 지정하지 않으면 작성 요청을 받은 날짜를 사용한다. 예: `스터디_실습가이드_09월_02일.md`
- 사용자가 별도로 파일만 생성해 달라고 하지 않으면, 저장한 가이드의 내용도 대화에 함께 출력한다.
